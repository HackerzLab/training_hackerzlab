  <div class="container" id="hackers">
    <div class="jumbotron">
      <h1>HackerzLab.博多</h1>
      <p>ホワイトハカーというのを知ってるかい？...</p>
    </div>
  </div>
  <!-- body msg -->
  <div class="container">
  % if ( my $msg = flash('msg') ) {
    <div class="alert alert-success"><b><%== $msg =%></b></div>
  % }
  % if ( my $msg = stash('msg') ) {
    <div class="alert alert-success"><b><%== $msg =%></b></div>
  % }
  </div>
